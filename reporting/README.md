# Reporting

Place Quarto assessment report sources and generated report-specific assets here.
The model run artifacts are expected under `analysis/outputs/` and ADMB
comparison artifacts under `analysis/comparisons/`.
